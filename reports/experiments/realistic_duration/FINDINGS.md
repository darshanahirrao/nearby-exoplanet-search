# Prepared, not executed

The 180-run six-star plan was frozen while the development benchmark was running.
The development gate subsequently failed. Accordingly, this assessment has not
been executed, and it supplies no recovery result or new-method view of these
stars. Do not describe the prepared injections as completed tests.

The physical-model code separately passed numerical checks against the uniform
central-transit depth and contact geometry, symmetry, and a zero-depth input.
These are model checks, not a successful search validation.

The [plan](plan.json) preserves cases, catalogue and telescope-input hashes,
software versions and the selection rule. The [protocol](../../../docs/EXPERIMENT_REALISTIC_DURATION.md)
and executable remain available for inspection. The runner refuses assessment
when the preceding development gate has failed.
