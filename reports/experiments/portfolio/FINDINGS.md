# Larger candidate shortlist: comparison in progress

The six initial development runs completed without errors. The leave-one-event-out
shortlist recovers both previously missed injections and retains the positive
control under all checks. The equal-budget local-total shortlist retains the
control but recovers neither missed injection. The predeclared smoke condition
therefore permits the full 120-run comparison, which is now executing.

This is still exposed development data. Some other fits also pass screening
without matching the injected signal. The six unmodified-star controls and all
lost-recovery checks remain essential; the smoke result does not justify a new
real-data search, calibrated false-alarm rate, method novelty or planet claim.

Both methods retain at most 32 eligible fits from one 128-refinement training
pass. Lower-ranked fits sharing over half of the smaller set of in-transit
training samples are removed. A separate software fixture verifies retention
of three strong injected planets and independence from held-sector flux. The
overlap rule can merge coincident commensurate signals and needs sensitivity
assessment.

The [protocol](../../../docs/EXPERIMENT_PORTFOLIO.md), [plan](plan.json) and
[six initial outcomes](smoke_results.json) freeze the choice and preserve its
limits. All earlier failed gates and the original 100-star pilot remain unchanged.
